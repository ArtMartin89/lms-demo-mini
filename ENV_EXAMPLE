# Database Configuration
POSTGRES_USER=lms_user
POSTGRES_PASSWORD=lmsDemosecure25
POSTGRES_DB=lms_db
DATABASE_URL=postgresql://lms_user:your_secure_password_here@db:5432/lms_db

# Backend Configuration
SECRET_KEY=your-secret-key-change-in-production-min-32-chars
BACKEND_PORT=8000
STORAGE_PATH=/app/storage

# Frontend Configuration
# ВАЖНО: Для VPS используйте IP адрес сервера, а не localhost!
# Узнайте IP: curl ifconfig.me
REACT_APP_API_URL=http://YOUR_SERVER_IP:8000/api/v1
# Для production с доменом:
# REACT_APP_API_URL=http://your-domain.com/api/v1
# или для HTTPS:
# REACT_APP_API_URL=https://your-domain.com/api/v1
FRONTEND_PORT=3000

# CORS Configuration
# ВАЖНО: Для VPS используйте IP адрес сервера, а не localhost!
# Разделите несколько origins запятыми
CORS_ORIGINS=http://YOUR_SERVER_IP:3000
# Для production с доменом:
# CORS_ORIGINS=http://your-domain.com,https://your-domain.com

# Server Configuration (для VPS)
DOMAIN=your-domain.com
PROTOCOL=http
# или для HTTPS:
# PROTOCOL=https

# Nginx Configuration
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

